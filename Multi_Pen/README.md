## Setup

```bash
uv sync
# or: pip install -e .
```

## Running Experiments

Generating the data takes approximately 10 minutes and costs around $20.

```bash
python3 generate_data.py
```

To generate the plots, run

```bash
python3 plot.py
```
